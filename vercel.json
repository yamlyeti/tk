{
  "buildCommand": "npm run build",
  "devCommand": "vite --port $PORT --strictPort",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/|@vite|@react-refresh|src/|node_modules/|.*\\.[a-zA-Z0-9]+$).*)", "destination": "/index.html" }
  ]
}
